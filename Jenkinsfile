pipeline {
    agent any

    stages {

        stage('Hello') {
            steps {
                echo 'Jenkins Connected Successfully'
            }
        }

        stage('Python Version') {
            steps {
                sh 'python --version'
            }
        }
    }
}
